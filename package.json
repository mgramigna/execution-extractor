{
  "name": "execution-extractor",
  "version": "0.0.3",
  "bin": {
    "exec-extract": "dist/index.js"
  },
  "files": [
    "dist/*"
  ],
  "scripts": {
    "build": "tsc && copyfiles \"./template/**/*.{js,json}\" dist",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/fhir": "^0.0.35",
    "@types/inquirer": "^8.2.5",
    "@types/node": "^18.13.0",
    "copyfiles": "^2.4.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "inquirer": "^8.2.5",
    "inquirer-path": "^1.0.0-beta5"
  }
}
